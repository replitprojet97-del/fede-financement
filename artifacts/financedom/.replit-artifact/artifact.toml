kind = "web"
previewPath = "/"
title = "FinanceDOM — Subventions non remboursables"
version = "1.0.0"
id = "artifacts/financedom"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24957

[services.development]
run = "pnpm --filter @workspace/financedom run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/financedom", "run", "build" ]
publicDir = "artifacts/financedom/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24957"
BASE_PATH = "/"
